export enum MessageType {
    Create,
    Read,
    Update,
    Delete,
    Custom
}

export enum ToastType{
    Info,
    Success,
    Warning,
    Error
}

export enum EventDialogType{
    Info,
    Success,
    Warning,
    Error
}